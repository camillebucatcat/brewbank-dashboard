import type { Meta, StoryObj } from '@storybook/react';

import { {{pascalCase namespace}} } from './{{pascalCase namespace}}';

const meta = {
  title: '{{pascalCase namespace}}',
  component: {{pascalCase namespace}},
  parameters: {
    layout: 'centered',
  },
  argTypes: {
    // control example
    // theme: {
    //   options: ['primary', 'secondary'],
    //   control: { type: 'select' },
    // },
    // disabled: {
    //   control: { type: 'boolean' },
    // },
  },
} satisfies Meta<typeof {{pascalCase namespace}}>;

export default meta;
type Story = StoryObj<typeof meta>;

export const Default: Story = {
  args: {
    // add default values
  },
};
